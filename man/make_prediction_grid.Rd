% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_prediction_grid}
\alias{make_prediction_grid}
\title{Build a prediction grid over two focal variables}
\usage{
make_prediction_grid(
  model,
  x,
  y,
  facet_by = NULL,
  n = 50,
  n_bins = 3,
  bin_method = "quantile"
)
}
\arguments{
\item{model}{A fitted model object.}

\item{x}{Character. First focal variable (mapped to x-axis).}

\item{y}{Character. Second focal variable (mapped to y-axis).}

\item{facet_by}{Character vector or NULL. Conditioning variable(s) whose
levels generate separate surfaces.}

\item{n}{Integer. Grid density for continuous axes.}

\item{n_bins}{Integer. Number of bins for continuous \code{facet_by} variables.}

\item{bin_method}{Character. Binning method: "quantile", "equal", or "pretty".}
}
\value{
A list with components:
\describe{
\item{grid}{data.frame suitable for \code{predict()}}
\item{binned_by}{named list of bin info for continuous facet_by variables}
}
}
\description{
For continuous variables, generates a regular sequence across the observed
range. For categorical/factor variables, uses all observed levels. Continuous
\code{facet_by} variables are binned, and predictions use bin midpoints.
}
\examples{
\donttest{
dat = sim_factorial(design = "mixed", seed = 42)
fit = lm(y ~ temp * pressure * catalyst, data = dat)
result = make_prediction_grid(fit, x = "temp", y = "pressure",
                              facet_by = "catalyst", n = 10)
str(result$grid)
}

}
