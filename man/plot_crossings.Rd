% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossings.R
\name{plot_crossings}
\alias{plot_crossings}
\title{Plot Crossing Regions as a Standalone 3D Scatter}
\usage{
plot_crossings(
  model,
  x,
  y,
  facet_by,
  n = 50,
  n_bins = 3,
  bin_method = "quantile",
  tolerance = NULL,
  labs = NULL,
  title = NULL,
  marker_size = 3,
  marker_opacity = 0.7
)
}
\arguments{
\item{model}{A fitted model object.}

\item{x}{Character. First focal variable.}

\item{y}{Character. Second focal variable.}

\item{facet_by}{Character vector. Conditioning variable(s).}

\item{n}{Integer. Grid resolution (default 50).}

\item{n_bins}{Integer. Bins for continuous facet_by (default 3).}

\item{bin_method}{Character. Binning method.}

\item{tolerance}{Numeric or NULL. Crossing detection tolerance.}

\item{labs}{Named list for axis labels, e.g.,
\code{list(x = "Temperature", y = "Pressure", z = "Yield")}.}

\item{title}{Character or NULL. Plot title.}

\item{marker_size}{Numeric. Marker size (default 3).}

\item{marker_opacity}{Numeric in \code{[0, 1]}. Marker opacity (default 0.7).}
}
\value{
A \code{plotly} htmlwidget object. If no crossings are found,
returns an empty plot with a "No crossings detected" annotation.
}
\description{
Visualizes only the crossing points between interaction surfaces as an
interactive 3D scatter plot, color-coded by surface pair. This isolates
where interaction effects are strongest, without the surfaces themselves.
}
\examples{
\donttest{
dat = sim_factorial(design = "mixed", seed = 42)
fit = lm(y ~ temp * pressure * catalyst, data = dat)
plot_crossings(fit, "temp", "pressure", "catalyst")

# with custom labels
plot_crossings(fit, "temp", "pressure", "catalyst",
               labs = list(x = "Temp (C)", y = "Press (psi)", z = "Yield"))
}

}
