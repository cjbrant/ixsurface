% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_surface.R
\name{model_surface}
\alias{model_surface}
\title{Visualize and compare prediction surfaces from multiple models}
\usage{
model_surface(
  models,
  x,
  y,
  mode = c("surface", "fitted", "residual"),
  n = 50,
  alpha = 0.5,
  show_observed = TRUE,
  labs = NULL,
  title = NULL,
  theme = "default",
  ...
)
}
\arguments{
\item{models}{Named list of fitted model objects. Each must have a
\code{predict} method recognized by \code{\link[=safe_predict]{safe_predict()}}.}

\item{x}{Character. Variable name mapped to the x-axis. Must be a
predictor in all models.}

\item{y}{Character. Variable name mapped to the y-axis. Must be a
predictor in all models.}

\item{mode}{Character. Visualization mode: \code{"surface"} (default),
\code{"fitted"}, or \code{"residual"}.}

\item{n}{Integer. Grid resolution per continuous axis (default 50).}

\item{alpha}{Numeric in \verb{[0, 1]}. Surface opacity (default 0.5).}

\item{show_observed}{Logical. Overlay observed data points (default \code{TRUE}).}

\item{labs}{Named list for axis labels, e.g.
\code{list(x = "Weight", y = "Year", z = "MPG")}.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{theme}{Character. Color theme: \code{"default"}, \code{"viridis"}, or
\code{"grey"}.}

\item{...}{Reserved for future use.}
}
\value{
A \code{plotly} htmlwidget object.
}
\description{
Renders interactive 3D plotly surfaces showing how multiple fitted
models predict across a 2D predictor grid. Where surfaces diverge,
models disagree about the response — indicating regions where model
class affects predictions. Companion to \code{\link[=interaction_surface]{interaction_surface()}}, which
compares factor levels within a single model; this function compares
models on the same predictors.
}
\details{
Three rendering modes are available:
\itemize{
\item \code{"surface"} shows smooth prediction surfaces, one per model.
Answers: what does each model think the function looks like?
\item \code{"fitted"} shows each model's predictions at observed \verb{(x, y)}
locations as colored points. Answers: where does each model land
relative to the data?
\item \code{"residual"} shows vertical segments from each observation to one
model's fitted value. Answers: how big are this model's errors,
and where? Only one model at a time in this mode.
}

Non-focal predictors are held at their median (continuous) or mode
(categorical), using \code{\link[=make_prediction_grid]{make_prediction_grid()}}. The same convention
as \code{\link[=interaction_surface]{interaction_surface()}}.

For categorical predictors, integer-coded axes are used internally
with level names as tick labels, matching \code{\link[=interaction_surface]{interaction_surface()}}
behavior.

All models in \code{models} must share the same response variable (extracted
from the first model via \code{model.frame()}). Mixing models with different
responses produces nonsense comparisons and is not checked.
}
\examples{
\donttest{
dat = sim_factorial(n = 300, design = "continuous", seed = 42)
fit_lm = lm(y ~ temp * pressure, data = dat)
fit_gam = mgcv::gam(y ~ s(temp) + s(pressure), data = dat)

model_surface(
  models = list(linear = fit_lm, gam = fit_gam),
  x = "temp", y = "pressure"
)
}

}
\seealso{
\code{\link[=interaction_surface]{interaction_surface()}} for within-model factor comparison,
\code{\link[=model_dominance]{model_dominance()}} for regional performance comparison.
}
