% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_residuals.R
\name{extract_residuals}
\alias{extract_residuals}
\title{Extract honest residuals from a fitted model}
\usage{
extract_residuals(model, data, response, verbose = TRUE)
}
\arguments{
\item{model}{A fitted model object.}

\item{data}{The original data frame the model was fit on.}

\item{response}{Character. Name of the response variable in \code{data}.}

\item{verbose}{Logical. If \code{TRUE} (default), emit warnings about the
residual source.}
}
\value{
A list with:
\describe{
\item{\code{residuals}}{Numeric vector of signed residuals, length
\code{nrow(data)}.}
\item{\code{source}}{Character: \code{"cv"} or \code{"training"}.}
\item{\code{description}}{Human-readable description of the
residual source.}
}
}
\description{
Extracts residuals for use in performance-based visualizations. The
source of residuals depends on the model object type:
}
\details{
\itemize{
\item For \code{caret} \code{train} objects with \code{savePredictions = "final"},
returns cross-validated residuals.
\item For \code{caret} \code{train} objects without saved predictions, returns
training residuals with a warning.
\item For other model objects (base \code{lm}, \code{glm}, \code{rlm}, \code{gam}, etc.),
returns training residuals with a warning.
}

The returned residuals are signed (observed - predicted), aligned with
the original data row order.

For honest regional model comparison, cross-validated residuals are
required. Training residuals on flexible models (like kNN) are
systematically smaller than true generalization error, which would
bias performance comparisons toward flexible models.

To get CV residuals from caret, fit with \code{savePredictions = "final"}
in \code{trainControl()}.
}
\examples{
\donttest{
tr = caret::trainControl(method = "cv", number = 10,
                         savePredictions = "final")
fit = caret::train(mpg ~ weight + year, data = Auto,
                   method = "lm", trControl = tr)
result = extract_residuals(fit, Auto, "mpg")
result$source  # "cv"
}

}
