% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_dominance.R
\name{model_dominance}
\alias{model_dominance}
\title{Map which model performs best across the predictor space}
\usage{
model_dominance(
  models,
  x,
  y,
  response,
  data = NULL,
  n = 60,
  k_nearest = 5,
  show_points = TRUE,
  show_ties = TRUE,
  tie_threshold = 0.05,
  clip_to_hull = TRUE,
  palette = "Set2",
  labs = NULL,
  title = NULL,
  verbose = TRUE
)
}
\arguments{
\item{models}{Named list of fitted model objects. Each must have a
\code{predict} method. For honest comparison, use \code{caret::train()}
objects fit with \code{savePredictions = "final"} in \code{trainControl()}.}

\item{x}{Character. Variable name mapped to the x-axis. Must be a
predictor in all models and a numeric column in \code{data}.}

\item{y}{Character. Variable name mapped to the y-axis. Must be a
predictor in all models and a numeric column in \code{data}.}

\item{response}{Character. Name of the response variable.}

\item{data}{Data frame containing observed predictor values and
response. If \code{NULL} (default), \code{model.frame(models[[1]])} is used.}

\item{n}{Integer. Grid resolution per axis (default 60).}

\item{k_nearest}{Integer. Number of nearest observed points to average
over for each grid cell (default 5). Higher = smoother regions.
Acts as a bandwidth parameter.}

\item{show_points}{Logical. Overlay observed data points on the
heatmap (default \code{TRUE}).}

\item{show_ties}{Logical. Mark grid cells where two or more models
have nearly equal residuals as \code{"tied"} instead of picking a
winner (default \code{TRUE}).}

\item{tie_threshold}{Numeric. Two models are considered tied if their
mean residuals differ by less than this fraction of the response
standard deviation (default 0.05).}

\item{clip_to_hull}{Logical. Restrict heatmap to the convex hull of
observed data (default \code{TRUE}).}

\item{palette}{Character. Color palette from RColorBrewer (default
\code{"Set2"}).}

\item{labs}{Named list for axis labels.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{verbose}{Logical. Emit warnings about residual sources
(default \code{TRUE}).}
}
\value{
A \code{ggplot2} plot object. The grid data with winners is
accessible via \code{plot$data}.
}
\description{
Renders a 2D heatmap of the predictor space, colored by which model
has the smallest mean absolute residual in each region. Uses honest
(cross-validated) residuals when available via \code{\link[=extract_residuals]{extract_residuals()}},
falling back to training residuals with a warning otherwise.
}
\details{
For each grid cell, the function finds nearby observed points and
identifies the model with the smallest mean absolute residual at
those points. The heatmap is clipped to the convex hull of the
observed data by default, since regions far from observations don't
have meaningful "winners".

Companion to \code{\link[=model_surface]{model_surface()}} (which visualizes model behavior) —
this function visualizes model performance.

This function does \emph{not} compute predictions on a grid. Instead, it
uses each model's residuals at the observed data points (CV residuals
when available) to estimate regional performance via nearest-neighbor
averaging. This is more honest than grid-based prediction because
it doesn't extrapolate model behavior to regions where no data exists.

The \code{k_nearest} parameter acts as a smoothing bandwidth:
\itemize{
\item Small \code{k_nearest} (1-5): high resolution, more noise-sensitive
\item Medium \code{k_nearest} (5-25): typical use, balances local detail
with smoothing
\item Large \code{k_nearest} (\eqn{\to n}): converges toward global
comparison, regional structure is lost
}
}
\examples{
\donttest{
tr = caret::trainControl(method = "cv", number = 10,
                         savePredictions = "final")

fit_lm = caret::train(mpg ~ weight + year, data = Auto,
                      method = "lm", trControl = tr)
fit_knn = caret::train(mpg ~ weight + year, data = Auto,
                       method = "knn", trControl = tr,
                       tuneGrid = data.frame(k = 20))

model_dominance(
  models = list(OLS = fit_lm, kNN = fit_knn),
  x = "weight", y = "year",
  response = "mpg", data = Auto
)
}

}
\seealso{
\code{\link[=model_surface]{model_surface()}} for visualizing model behavior,
\code{\link[=extract_residuals]{extract_residuals()}} for the underlying residual extraction.
}
